# ==============================
# Dory Environment Configuration
# ==============================
# This file is an example template. Copy to `.env` and adjust if needed.
# For quick local Docker usage, only AI key is optional.

# ------------------------------
# AI Provider (optional)
# If not set, Copilot features will be disabled.
# Default: OpenAI official API
# ------------------------------
DORY_AI_PROVIDER=openai
DORY_AI_MODEL=gpt-4o-mini
DORY_AI_API_KEY=
DORY_AI_URL=https://api.openai.com/v1

# Optional: remote Dory Cloud API endpoint
# Example: https://app.getdory.dev/api
NEXT_PUBLIC_DORY_CLOUD_API_URL=

# ------------------------------
# Authentication
# Local single-user Docker usage can keep defaults.
# For production, replace with secure random strings. generate with: openssl rand -hex 32
# ------------------------------
BETTER_AUTH_SECRET=b68fa3d2ab59062380386821f4e9fae3844f7a9f37ea98c94b6ffc1f4f5ec10d
BETTER_AUTH_URL=http://localhost:3000

# ------------------------------
# Encryption Secret
# Used to encrypt stored sensitive data (connection passwords, etc.)
# For production, generate: openssl rand -base64 32
# ------------------------------
DS_SECRET_KEY=PEhyv+Y9pUuBl0IAm6K91cpa5OMBj1Mudt1Bx3IM5AE=

# ------------------------------
# Email (optional)
# Only required if enabling email verification or invitations.
# ------------------------------
RESEND_API_KEY=

# ------------------------------
# GitHub OAuth (optional)
# Only required if enabling GitHub login.
# ------------------------------
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ------------------------------
# Database
# Default uses embedded PGLite with persistent volume.
# ------------------------------
DB_TYPE=pglite
PGLITE_DB_PATH=/data/dory

# ------------------------------
# External Postgres (optional)
# Uncomment if using Postgres instead of PGLite
# ------------------------------
# DB_TYPE=postgres
# DATABASE_URL="postgresql://user:password@host:5432/dbname"

# ------------------------------
# Runtime mode
# Server-side runtime should use DORY_RUNTIME.
# For desktop app builds, set: DORY_RUNTIME=desktop
# ------------------------------
DORY_RUNTIME=

# ------------------------------
# Connection timeouts
# CONNECT/TEST uses CONNECTION_REQUEST_TIMEOUT_MS.
# Regular ClickHouse queries use QUERY_REQUEST_TIMEOUT_MS.
# ------------------------------
CONNECTION_REQUEST_TIMEOUT_MS=20000
QUERY_REQUEST_TIMEOUT_MS=120000

TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000


# ------------------------------
# PostHog Analytics (optional)
# Only required if you want to enable analytics.
# ------------------------------
NEXT_PUBLIC_POSTHOG_KEY=your_posthog_key_here
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
